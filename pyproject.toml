[project]
name = "text-constellation"
version = "0.2.0"
description = "Turn text into deterministic connected constellation SVGs and analysis manifests"
requires-python = ">=3.10"
license = "MIT"

[tool.ruff]
line-length = 120
